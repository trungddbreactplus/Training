[project]
name = "python-assignments"
version = "0.1.0"
description = "Python assignments and mini projects"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "requests>=2.33.1",
    "tqdm>=4.67.1",
]
